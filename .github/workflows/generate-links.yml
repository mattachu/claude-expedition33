name: Generate dynamic listing files

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  generate-links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate scripts/scripts.md
        run: python3 scripts/generate_scripts_md.py

      - name: Generate LINKS.md
        run: python3 scripts/generate_links.py ${{ github.sha }}

      - name: Commit LINKS.md
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add LINKS.md scripts/scripts.md
          git diff --staged --quiet || git commit -m "chore: update links and scripts reference [skip ci]"
          git push
